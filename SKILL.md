---
name: math-review-deck
description: >
  專為高三學生複習澳門四校聯考（正卷與附加卷）打造的互動視覺化投影簡報技能（純前端、零金鑰、零建置）。
  將歷屆真題與核心考點，做成「一頁一題、左題右解答、題目下方呈現核心知識點、適當加上動態圖拖滑桿就會動」的網頁投影簡報，並可一鍵部署上線。

  當使用者說「做四校聯考複習簡報」「澳門四校聯考複習網頁」「正卷附加卷一頁一題複習」
  「幫高三學生做四校聯考教材」「做一個可以拖滑桿的聯考試題簡報」「把正卷/附加卷題目做成左題右解」
  「做一份複習簡報」「把這一冊做成互動網頁」「做會考／段考複習網頁」，或要求維護、
  新增四校聯考試題與複習簡報時，請一定要使用此技能。

  適用情境：課堂投影上課、老師在畫面上用雷射筆與畫筆講解、一鍵按 A 鍵顯示/隱藏規範步驟解答、
  題目與圖解可滿版全螢幕放大至黑板講解。

  產出為單一資料夾的靜態網頁（index.html + engine.js + style.css + svg.js + ch-jae.js），
  可直接用瀏覽器開，或部署到 Cloudflare Pages／Netlify／GitHub Pages。
  數學排版用 MathJax，圖形為原生 SVG，不需要任何 API 金鑰。
---

# 澳門四校聯考（JAE）互動視覺化複習簡報（math-review-deck）

本技能專為**高三學生複習「澳門四校聯考（正卷與附加卷）」**而設計。
核心特點：**一頁一題、左題右解答、題目正下方呈現核心知識點、動態幾何/函數拖滑桿即時探究**。

---

## 1. 核心版面結構（左題右解答）

每張投影片即為一道四校聯考真題或精選例題：

- **左欄：試題與考點欄（分開為獨立雙卡片）**
  - **頂部三部曲動線列**：`① 審題閱讀` ➔ `② 核心考點&公式` ➔ `③ 規範解答與速解`（可直接點選切換或依序推進）。
  - **區域 1 (左上)：試題題幹與選項卡片**
    - 題標與徽章：年份、卷別、題號、主題標籤、分值。
    - 🔍 放大題目按鈕（快捷鍵 `Q`）：全螢幕超大字體呈現題幹與選項。
    - 題幹文字與選項網格（MathJax 渲染）。
  - **區域 2 (左下)：核心知識點 & 必背公式卡片（獨立區域，支援放大）**
    - 預設隱藏引導：初翻至該題時顯示提示卡，按下一頁或快捷鍵 `K` 揭曉。
    - 🔍 放大知識點按鈕：全螢幕放大必背公式卡片、聯考切入點與易錯盲區，方便黑板前推導。
    - `formulas`：本題必備高頻公式塊（MathJax 精美卡片排版）。
    - `points`：解題核心思路、四校聯考出題特徵與秒殺觀察點。
    - `pitfall`：⚠️ 易錯警示（考生常犯扣分細節、符號約定、非零或定義域陷阱）。
- **右欄：解答與動態探究欄（區域 3）**
  - **動態互動圖形（visual）**：幾何/三角/微積分/複數題型配備原生 SVG 動態圖與滑桿（Slider），拖動滑桿即時改變圖形參數，驗證幾何定理。
  - **解題思維與規範步驟卡片**：
    - 預設隱藏，按下一頁或快捷鍵 `A` 展開解答（課堂先提問、再思考、後出解）。
    - 🔍 放大解答按鈕：全黑板滿版呈現解題步驟，方便老師用畫筆精講踩點評分標準。
    - `thinking`：【解題思路】大綱導引。
    - `steps`：符合四校聯考官方閱卷標準的規範踩點步驟。
    - `ans`：參考答案醒目徽章。
    - `quickTip`：⚡ 聯考速解訣竅（選擇題秒殺法、代數檢驗法）。

- **三部曲漸進式翻頁體驗（Pedagogical Step Flow）**：
  - **階段 0（翻到新題目）**：左上僅展示題目題幹與選項，左下核心知識點與右欄規範解答呈現引導卡，方便老師提問。
  - **階段 1（按 Next / → / Space / PageDown 或 K）**：平滑揭曉「核心知識點 & 必背公式」，老師引導學生回顧概念。
  - **階段 2（再按 Next / → / Space / PageDown 或 A）**：平滑揭曉「解題思維與規範步驟」，展示完整踩點步驟與速解。
  - **階段 3（再按 Next）**：順暢進入下一道試題（重新以階段 0 開始）。
  - **倒退（Prev / ← / PageUp）**：依序回退上一階段（若回到上一題，自動呈現已展開狀態）。

---

## 2. 試題資料規格範例

在各章試題檔 `ch-jae.js` 或 `ch-2024-standard.js` 中定義試題物件：

```javascript
window.DECK = window.DECK || [];
DECK.push({
  ch: "2024-正卷",
  title: "2024 澳門四校聯考 數學正卷精選",
  color: "#2563eb",
  sections: ["選擇題 第7題 · 三角函數與正弦定理", "選擇題 第11題 · 解析幾何弦長"],
  slides: [
    {
      year: "2024",
      paper: "正卷",
      qNum: "選擇題 第7題",
      topic: "三角函數 · 正弦定理與外接圓",
      score: "4分",
      q: "在 $\\triangle ABC$ 中，已知 $A = 30^\\circ$，$a = 4$，則 $\\triangle ABC$ 的外接圓直徑為？",
      options: ["(A) 4", "(B) 6", "(C) 8", "(D) 10"],
      
      // 題目正下方的核心知識點
      knowledge: {
        formulas: ["\\frac{a}{\\sin A} = 2R"],
        points: [
          "<b>正弦定理核心</b>：任一邊與其對角正弦之比等於外接圓直徑 $2R$。",
          "<b>聯考思維導向</b>：題幹出現「外接圓」，優先聯想正弦定理。"
        ],
        pitfall: "審題注意問的是「直徑 $2R$」還是「半徑 $R$」，切勿漏除以 2 或手快選錯！"
      },

      // 動態互動探究（SVG + 滑桿）
      visual: function(host) {
        host.innerHTML = `
          <div id="vis-svg"></div>
          <div class="ictrl">
            <label>頂點 A 位置：</label>
            <input type="range" id="sl" min="40" max="140" value="90">
          </div>
        `;
        const sl = host.querySelector('#sl');
        sl.oninput = () => { /* 依 sl.value 即時重繪 SVG */ };
      },

      // 規範解答（右欄）
      solution: {
        thinking: "利用正弦定理公式直接代入 $A=30^\\circ$ 與 $a=4$。",
        steps: [
          "由正弦定理可知：$\\frac{a}{\\sin A} = 2R$。",
          "代入已知條件：$$2R = \\frac{4}{\\sin 30^\\circ} = \\frac{4}{0.5} = 8$$"
        ],
        ans: "(C)",
        quickTip: "特殊角 $30^\\circ$ 正弦值為 $0.5$，直徑等於邊長乘 2，口算 3 秒選 (C)。"
      }
    }
  ]
});
```

---

## 3. 工具腳本與指令

### 產生全新聯考簡報專案
```bash
# 產生包含正卷與附加卷的骨架
python scripts/new_jae_deck.py --out jae-2024 --year 2024 --paper both

# 僅產生正卷
python scripts/new_jae_deck.py --out jae-standard --year 2024 --paper standard

# 僅產生附加卷
python scripts/new_jae_deck.py --out jae-supp --year 2024 --paper supplementary
```

### 驗證試題結構與動態視覺
```bash
# 驗證指定檔案或整個專案資料夾
node scripts/verify_jae_deck.js examples/ch-jae-example.js
node scripts/verify_jae_deck.js demo
```

### 本機預覽
```bash
python -m http.server 8000 --directory demo
# 瀏覽器開啟 http://localhost:8000 即可全螢幕投影測試
```

---

## 4. 授課教具與操作指引

- **`Space` / `→` / `PageDown`**：漸進式推進下一階段（① 題目 ➔ ② 考點公式 ➔ ③ 規範解答 ➔ 下一題）。
- **`←` / `PageUp`**：依序倒退上一階段。
- **快速鍵 `K`**：一鍵展開／收起左下「核心知識點 & 必背公式」。
- **快速鍵 `A`**：一鍵展開／收起右欄「規範解答與速解」。
- **快速鍵 `Q`**：一鍵開啟「試題題幹全螢幕放大」。
- **🔍 放大按鈕**：
  - 左上 `🔍 放大題目`：全螢幕放大題目題幹與選項。
  - 左下 `🔍 放大知識點`：全螢幕放大必背公式卡片、聯考切入點與易錯盲區。
  - 右側 `🔍 放大解答`：全黑板滿版呈現解題規範步驟與評分標準。
- **`L` 鍵**：開啟／關閉虛擬紅色雷射筆（含動態拖尾效果）。
- **`P` 鍵**：開啟／關閉板書塗鴉畫筆（多色切換、擦子）。
- **`C` 鍵**：一鍵清除當前試題筆跡。
- **一鍵部署**：推送到 GitHub 即透過 GitHub Actions 自動部署至 GitHub Pages。

---

## 5. 課堂進度控制教師面板（防提前看答案）

專為「以學生進度為第一原則、避免學生提前查看後續試卷與解答」設計：

- **啟動入口**：點擊網頁右上角 **`⚙️ 課堂進度控制`** 按鈕（封面與題目舞台皆有）。
- **教師身分驗證**：輸入教師 PIN 碼（預設：**`8888`**），驗證成功後開啟管理視窗。
- **快速進度按鈕（依教學週次）**：
  - `【第 1、2 週】` ➔ 2025 正卷 & 附加卷（25 題）
  - `【第 3、4 週】` ➔ 2024 正卷 & 附加卷（25 題）
  - `【第 5、6 週】` ➔ 2023 正卷 & 附加卷（25 題）
  - `【第 7、8 週】` ➔ 2022 正卷 & 附加卷（25 題）
  - `【第 9、10 週】` ➔ 2021 正卷 & 附加卷（25 題）
  - `【前 4 週累計】` / `【前 6 週累計】` / `【全部開放 (10 卷)】` / `【全部清空】`
- **5 年 10 卷自由勾選清單**：可自由獨立勾選任意年份的正卷與附加卷。
- **📱 大螢幕 QR 碼**：一鍵彈出純前端 SVG 向量二維碼，學生拿 iPad 相機掃碼直接載入指定開放的試卷（零外部 API，離線可用）。
- **📋 複製學生網址**：自動生成帶 `?papers=2025-std,2025-sup` 的學生專用受限連結。
- **💾 儲存並在當前生效**：即時更新當前設備的題庫與目錄，免手動重新整理頁面。

